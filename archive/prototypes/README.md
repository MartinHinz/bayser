# Prototype scripts

This directory contains early exploratory scripts that are no longer part of the maintained BaySer workflow.

They are kept temporarily for reference during the transition from prototype code to the package-based CLI.
The maintained entry point is now:

```bash
bayser ...